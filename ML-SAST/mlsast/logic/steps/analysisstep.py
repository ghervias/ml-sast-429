from mlsast.logic.steps.basestep import BaseStep

class AnalysisStep(BaseStep):
    """Base class for all steps that perform some form of analysis.

    Args:
        BaseStep (BasStep): The base class.
    """
