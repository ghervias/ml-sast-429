from mlsast.logic.steps.basestep import BaseStep

class GraphStep(BaseStep):
    """Basic graph step that should be used as the base class for all steps that
    generate graphs.

    Args:
        BaseStep (BaseStep): Base class.
    """
